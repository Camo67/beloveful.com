# SPA fallback for client-side routing
/*    /index.html   200

# Notes:
# - Ensures pretty links like /africa and /europe/france render the SPA
# - Keep API/function routes above this line if needed
